server:
  port: 8123

app:
  id: dubbo-apollo-provider
apollo:
  meta: http://127.0.0.1:8080
  bootstrap:
    enabled: true
    namespaces: application, dubbo
    eagerLoad:
      enabled: true
logging:
  level:
    com: info